# Frontend runtime
# Use full API base if frontend and backend are deployed on different hosts.
# Example: https://api.example.com/api
VITE_API_BASE=

# Optional: force Google account picker
VITE_GOOGLE_FORCE_SELECT_ACCOUNT=false
